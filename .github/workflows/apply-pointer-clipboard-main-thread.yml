name: apply-pointer-clipboard-main-thread

on:
  push:
    branches: [fix/pointer-clipboard-main-thread]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: fix/pointer-clipboard-main-thread
          fetch-depth: 0

      - name: Apply patch
        run: python3 scripts/apply_pointer_clipboard_main_thread.py

      - name: Verify source
        run: |
          python3 scripts/verify_ball_position_state.py
          python3 .github/scripts/es5_scan.py
          git diff --check

      - name: Commit source
        shell: bash
        run: |
          if git diff --quiet -- code/th_17_pointer.js scripts/verify_ball_position_state.py; then
            echo "No source changes required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add code/th_17_pointer.js scripts/verify_ball_position_state.py
          git commit -m "修复主线程剪贴板写入链"
          git push origin HEAD:fix/pointer-clipboard-main-thread
