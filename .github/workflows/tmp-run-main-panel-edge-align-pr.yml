name: tmp-run-main-panel-edge-align-pr

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: tmp-run-main-panel-edge-align-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  patch:
    if: github.event.pull_request.head.ref == 'fix/main-panel-edge-align-cleanup-20260714'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repair branch
        uses: actions/checkout@v4
        with:
          ref: fix/main-panel-edge-align-cleanup-20260714
          fetch-depth: 0
          token: ${{ secrets.TOOLHUB_AUTOMATION_TOKEN || github.token }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Apply focused patch
        run: python3 scripts/tmp_fix_main_panel_edge_align.py

      - name: Verify changed module version
        run: |
          git fetch --no-tags origin main --depth=1
          python3 scripts/verify_changed_module_versions.py --base-ref origin/main

      - name: Verify Rhino ES5
        run: python3 .github/scripts/es5_scan.py

      - name: Verify JavaScript syntax
        run: python3 scripts/verify_js_syntax.py

      - name: Verify module audits
        run: |
          python3 scripts/verify_module_boundaries.py
          python3 scripts/report_dead_module_symbols.py --check DEAD_CODE_AUDIT.md
          python3 scripts/report_protected_wrapper_chains.py --check PROTECTED_WRAPPER_AUDIT.md
          python3 scripts/report_th15_extra_symbols.py --check TH15_EXTRA_AUDIT.md

      - name: Verify main panel grid sizing
        run: python3 scripts/verify_main_panel_grid_sizing.py

      - name: Verify main panel paging
        run: python3 scripts/verify_main_panel_paging.py

      - name: Verify main panel adaptive layout
        run: python3 scripts/verify_main_panel_adaptive_layout.py

      - name: Verify main panel visual tuning
        run: python3 scripts/verify_main_panel_visual_tuning.py

      - name: Verify main panel drag sort
        run: python3 scripts/verify_main_panel_drag_sort.py

      - name: Verify main panel runtime status
        run: python3 scripts/verify_main_panel_runtime_status.py

      - name: Verify main panel close lifecycle
        run: python3 scripts/verify_main_panel_close_lifecycle.py

      - name: Verify panel layout settings cleanup
        run: python3 scripts/verify_panel_layout_settings_cleanup.py

      - name: Verify legacy main panel cleanup
        run: python3 scripts/verify_legacy_main_panel_cleanup.py

      - name: Check whitespace
        run: git diff --check

      - name: Remove temporary files
        shell: bash
        run: |
          rm -f .github/workflows/tmp-fix-main-panel-edge-align.yml
          rm -f .github/workflows/tmp-run-main-panel-edge-align-pr.yml
          rm -f scripts/tmp_fix_main_panel_edge_align.py
          rm -f .tmp-main-panel-edge-align-trigger

      - name: Commit repair
        shell: bash
        env:
          TOOLHUB_AUTOMATION_TOKEN: ${{ secrets.TOOLHUB_AUTOMATION_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "完成主面板同侧边缘对齐并清理临时文件"
          if [ -n "$TOOLHUB_AUTOMATION_TOKEN" ]; then
            git remote set-url origin "https://x-access-token:${TOOLHUB_AUTOMATION_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          fi
          git push origin HEAD:fix/main-panel-edge-align-cleanup-20260714
