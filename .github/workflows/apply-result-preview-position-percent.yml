name: Apply result preview position percent

on:
  push:
    branches:
      - agent/result-preview-position-percent

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/result-preview-position-percent
          fetch-depth: 0

      - name: Apply focused patch
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          def load(path):
              return Path(path).read_text(encoding="utf-8")

          def save(path, text):
              Path(path).write_text(text, encoding="utf-8")

          def replace_once(path, old, new):
              text = load(path)
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one match, got {count}: {old[:100]!r}")
              save(path, text.replace(old, new, 1))

          def sub_once(path, pattern, repl, flags=0):
              text = load(path)
              next_text, count = re.subn(pattern, repl, text, count=1, flags=flags)
              if count != 1:
                  raise SystemExit(f"{path}: regex expected one match, got {count}: {pattern[:100]!r}")
              save(path, next_text)

          # th_01_base.js: validator, defaults, schema and schema refresh rules.
          p = "code/th_01_base.js"
          replace_once(p, "// @version 1.1.15", "// @version 1.1.16")
          replace_once(
              p,
              '    POINTER_RESULT_PREVIEW_TIMEOUT_SEC: { type: "int", min: 1, max: 10, default: 3 },',
              '    POINTER_RESULT_PREVIEW_POSITION_PERCENT: { type: "int", min: 0, max: 100, default: 5 },\n'
              '    POINTER_RESULT_PREVIEW_TIMEOUT_SEC: { type: "int", min: 1, max: 10, default: 3 },'
          )
          replace_once(
              p,
              "        POINTER_RESULT_PREVIEW_TIMEOUT_SEC: 3,",
              "        POINTER_RESULT_PREVIEW_POSITION_PERCENT: 5,\n        POINTER_RESULT_PREVIEW_TIMEOUT_SEC: 3,"
          )
          replace_once(
              p,
              '        { key: "POINTER_RESULT_PREVIEW_TIMEOUT_SEC", name: "结果预览停留时间(秒)", type: "int", min: 1, max: 10, step: 1 },',
              '        { key: "POINTER_RESULT_PREVIEW_POSITION_PERCENT", name: "预览垂直位置(%)", desc: "按完整屏幕高度设置预览中心位置，实际显示会避开顶部和底部系统栏", type: "int", min: 0, max: 100, step: 1 },\n'
              '        { key: "POINTER_RESULT_PREVIEW_TIMEOUT_SEC", name: "结果预览停留时间(秒)", type: "int", min: 1, max: 10, step: 1 },'
          )
          replace_once(
              p,
              '            sStr.indexOf("POINTER_RESULT_PREVIEW_TIMEOUT_SEC") < 0 ||',
              '            sStr.indexOf("POINTER_RESULT_PREVIEW_POSITION_PERCENT") < 0 ||\n'
              '            sStr.indexOf("POINTER_RESULT_PREVIEW_TIMEOUT_SEC") < 0 ||'
          )
          replace_once(
              p,
              '                schemaItemDiffers("POINTER_RESULT_PREVIEW_TIMEOUT_SEC", ["name", "type", "min", "max", "step"]) ||',
              '                schemaItemDiffers("POINTER_RESULT_PREVIEW_POSITION_PERCENT", ["name", "desc", "type", "min", "max", "step"]) ||\n'
              '                schemaItemDiffers("POINTER_RESULT_PREVIEW_TIMEOUT_SEC", ["name", "type", "min", "max", "step"]) ||'
          )

          # th_05_persistence.js: saved value uses the dedicated preview-position effect.
          p = "code/th_05_persistence.js"
          replace_once(p, "// @version 1.0.6", "// @version 1.0.7")
          replace_once(
              p,
              '''    if (pointerChanged) {
      if (this.refreshPointerAfterSettingsChanged) this.refreshPointerAfterSettingsChanged();
      return;
    }''',
              '''    if (key === "POINTER_RESULT_PREVIEW_POSITION_PERCENT") {
      if (typeof this.onResultPreviewConfigurationChanged === "function") {
        this.onResultPreviewConfigurationChanged({
          positionOnly: true,
          clearPositionPreview: true,
          reason: "settings_commit"
        });
      }
      return;
    }

    if (pointerChanged) {
      if (this.refreshPointerAfterSettingsChanged) this.refreshPointerAfterSettingsChanged();
      return;
    }'''
          )

          # th_09_animation.js: abandoning old settings restores the persisted position.
          p = "code/th_09_animation.js"
          replace_once(p, "// @version 1.0.10", "// @version 1.0.11")
          replace_once(
              p,
              '''  if (!this.state.settingsPanel) return;

  this.safeRemoveView(this.state.settingsPanel, "settingsPanel");''',
              '''  if (!this.state.settingsPanel) return;

  try {
    if (typeof this.onResultPreviewConfigurationChanged === "function") {
      this.onResultPreviewConfigurationChanged({
        positionOnly: true,
        clearPositionPreview: true,
        reason: "settings_cancel"
      });
    }
  } catch(ePreviewPosition) {
    safeLog(this.L, "w", "restore result preview position on settings close fail: " + String(ePreviewPosition));
  }

  this.safeRemoveView(this.state.settingsPanel, "settingsPanel");'''
          )

          # th_13_panel_ui.js: only commit live positioning when the slider is released.
          p = "code/th_13_panel_ui.js"
          replace_once(p, "// @version 1.0.10", "// @version 1.0.11")
          replace_once(
              p,
              '      onStopTrackingTouch: function() { try { self.touchActivity();  } catch(e3) { safeLog(null, \'e\', "catch " + String(e3)); } }',
              '''      onStopTrackingTouch: function(seek) {
        try {
          self.touchActivity();
          if (String(item.key || "") === "POINTER_RESULT_PREVIEW_POSITION_PERCENT" &&
              typeof self.onResultPreviewConfigurationChanged === "function") {
            self.onResultPreviewConfigurationChanged({
              positionOnly: true,
              positionPreviewPercent: computeValByProgress(seek.getProgress()),
              reason: "settings_seek_stop"
            });
          }
        } catch(e3) { safeLog(null, 'e', "catch " + String(e3)); }
      }'''
          )

          # th_14_panels.js: keep position and timeout in the existing smart preview group.
          p = "code/th_14_panels.js"
          replace_once(p, "// @version 1.1.6", "// @version 1.1.7")
          replace_once(
              p,
              '''    {
      key: "result_preview",
      title: "结果预览",
      desc: "取字或框选 OCR 成功后，在状态栏下方显示两行预览；点击进入拾字",
      keys: [
        "POINTER_RESULT_PREVIEW_TIMEOUT_SEC"
      ]
    },''',
              '''    {
      key: "result_preview",
      title: "结果预览",
      desc: "取字或框选 OCR 成功后显示两行预览；可调整垂直位置和停留时间，点击进入拾字",
      keys: [
        "POINTER_RESULT_PREVIEW_POSITION_PERCENT",
        "POINTER_RESULT_PREVIEW_TIMEOUT_SEC"
      ]
    },'''
          )

          # th_16_entry.js: ToolApp resets and close paths must not leak a pending position override.
          p = "code/th_16_entry.js"
          replace_once(p, "// @version 1.0.16", "// @version 1.0.17")
          replace_once(
              p,
              '''    if (resetStack && r === "settings") {
      this.state.pendingUserCfg = null;''',
              '''    if (resetStack && r === "settings") {
      try {
        if (typeof this.onResultPreviewConfigurationChanged === "function") {
          this.onResultPreviewConfigurationChanged({
            positionOnly: true,
            clearPositionPreview: true,
            reason: "settings_reset"
          });
        }
      } catch(ePreviewReset) {}
      this.state.pendingUserCfg = null;'''
          )
          replace_once(
              p,
              '''  var s = this.state;
  if (!s) return true;
  var root = s.toolAppRoot ||''',
              '''  var s = this.state;
  if (!s) return true;
  try {
    if (typeof this.onResultPreviewConfigurationChanged === "function") {
      this.onResultPreviewConfigurationChanged({
        positionOnly: true,
        clearPositionPreview: true,
        reason: "toolapp_close"
      });
    }
  } catch(ePreviewPosition) {}
  var root = s.toolAppRoot ||'''
          )

          # th_21_result_preview.js: screen percentage positioning, safe insets and position-only updates.
          p = "code/th_21_result_preview.js"
          replace_once(p, "// @version 1.2.5", "// @version 1.3.0")
          replace_once(
              p,
              '''        copyFeedbackRunnable: null,
        handler: null,''',
              '''        copyFeedbackRunnable: null,
        configurationPositionRunnable: null,
        positionPreviewPercent: null,
        handler: null,'''
          )
          replace_once(
              p,
              '''    if (st.copyFeedbackRunnable === undefined) st.copyFeedbackRunnable = null;
    if (st.copyVisible === undefined) st.copyVisible = false;''',
              '''    if (st.copyFeedbackRunnable === undefined) st.copyFeedbackRunnable = null;
    if (st.configurationPositionRunnable === undefined) st.configurationPositionRunnable = null;
    if (st.positionPreviewPercent === undefined) st.positionPreviewPercent = null;
    if (st.copyVisible === undefined) st.copyVisible = false;'''
          )

          position_helpers = r'''  function screenFrame21(appObj) {
    var left = 0;
    var top = 0;
    var width = 0;
    var height = 0;
    var source = "";
    try {
      if (android.os.Build.VERSION.SDK_INT >= 30) {
        var wm = (appObj && appObj.state && appObj.state.wm) || context.getSystemService(android.content.Context.WINDOW_SERVICE);
        var metrics = wm.getCurrentWindowMetrics();
        var bounds = metrics.getBounds();
        if (bounds) {
          left = int21(bounds.left, 0);
          top = int21(bounds.top, 0);
          width = Math.max(0, int21(bounds.right, 0) - left);
          height = Math.max(0, int21(bounds.bottom, 0) - top);
          source = "window_metrics";
        }
      }
    } catch (eMetrics) {}
    if (width <= 0 || height <= 0) {
      try {
        var size = appObj && typeof appObj.getScreenSizePx === "function" ? appObj.getScreenSizePx() : null;
        if (size) {
          left = 0;
          top = 0;
          width = Math.max(0, int21(size.w, 0));
          height = Math.max(0, int21(size.h, 0));
          source = "real_metrics";
        }
      } catch (eSize) {}
    }
    if (width <= 0 || height <= 0) {
      try {
        var dm = context.getResources().getDisplayMetrics();
        left = 0;
        top = 0;
        width = Math.max(1, int21(dm.widthPixels, 1));
        height = Math.max(1, int21(dm.heightPixels, 1));
        source = "resource_metrics";
      } catch (eResource) {
        width = dp21(appObj, 360);
        height = dp21(appObj, 720);
        source = "fallback";
      }
    }
    return {
      left: left,
      top: top,
      right: left + Math.max(1, width),
      bottom: top + Math.max(1, height),
      width: Math.max(1, width),
      height: Math.max(1, height),
      source: source
    };
  }

  function readSafeInsets21(windowInsets) {
    var out = { top: 0, bottom: 0, left: 0, right: 0 };
    if (!windowInsets) return out;
    try {
      if (android.os.Build.VERSION.SDK_INT >= 30) {
        var types = android.view.WindowInsets.Type.statusBars() |
          android.view.WindowInsets.Type.navigationBars() |
          android.view.WindowInsets.Type.displayCutout();
        var value = null;
        try { value = windowInsets.getInsetsIgnoringVisibility(types); } catch (eIgnoring) {}
        if (!value) value = windowInsets.getInsets(types);
        if (value) {
          out.top = Math.max(out.top, int21(value.top, 0));
          out.bottom = Math.max(out.bottom, int21(value.bottom, 0));
          out.left = Math.max(out.left, int21(value.left, 0));
          out.right = Math.max(out.right, int21(value.right, 0));
        }
      } else {
        out.top = Math.max(out.top, int21(windowInsets.getSystemWindowInsetTop(), 0));
        out.bottom = Math.max(out.bottom, int21(windowInsets.getSystemWindowInsetBottom(), 0));
        out.left = Math.max(out.left, int21(windowInsets.getSystemWindowInsetLeft(), 0));
        out.right = Math.max(out.right, int21(windowInsets.getSystemWindowInsetRight(), 0));
      }
    } catch (eSystemInsets) {}
    try {
      var cutout = windowInsets.getDisplayCutout();
      if (cutout) {
        out.top = Math.max(out.top, int21(cutout.getSafeInsetTop(), 0));
        out.bottom = Math.max(out.bottom, int21(cutout.getSafeInsetBottom(), 0));
        out.left = Math.max(out.left, int21(cutout.getSafeInsetLeft(), 0));
        out.right = Math.max(out.right, int21(cutout.getSafeInsetRight(), 0));
      }
    } catch (eCutout) {}
    return out;
  }

  function systemSafeInsets21(root) {
    var result = { top: 0, bottom: 0, left: 0, right: 0, source: "none" };
    try {
      if (root && root.getRootWindowInsets) {
        var rootInsets = readSafeInsets21(root.getRootWindowInsets());
        result.top = Math.max(result.top, rootInsets.top);
        result.bottom = Math.max(result.bottom, rootInsets.bottom);
        result.left = Math.max(result.left, rootInsets.left);
        result.right = Math.max(result.right, rootInsets.right);
        if (result.top > 0 || result.bottom > 0 || result.left > 0 || result.right > 0) result.source = "root_insets";
      }
    } catch (eRootInsets) {}
    try {
      if (android.os.Build.VERSION.SDK_INT >= 30) {
        var wm = context.getSystemService(android.content.Context.WINDOW_SERVICE);
        var metricsInsets = readSafeInsets21(wm.getCurrentWindowMetrics().getWindowInsets());
        result.top = Math.max(result.top, metricsInsets.top);
        result.bottom = Math.max(result.bottom, metricsInsets.bottom);
        result.left = Math.max(result.left, metricsInsets.left);
        result.right = Math.max(result.right, metricsInsets.right);
        if (metricsInsets.top > 0 || metricsInsets.bottom > 0 || metricsInsets.left > 0 || metricsInsets.right > 0) result.source = "window_metrics_insets";
      }
    } catch (eMetricsInsets) {}
    if (result.top <= 0) {
      try {
        var statusId = context.getResources().getIdentifier("status_bar_height", "dimen", "android");
        if (statusId > 0) result.top = context.getResources().getDimensionPixelSize(statusId);
      } catch (eStatusResource) {}
    }
    if (result.bottom <= 0) {
      try {
        var navigationId = context.getResources().getIdentifier("navigation_bar_height", "dimen", "android");
        if (navigationId > 0) result.bottom = context.getResources().getDimensionPixelSize(navigationId);
      } catch (eNavigationResource) {}
    }
    result.top = Math.max(0, int21(result.top, 0));
    result.bottom = Math.max(0, int21(result.bottom, 0));
    result.left = Math.max(0, int21(result.left, 0));
    result.right = Math.max(0, int21(result.right, 0));
    return result;
  }

  function resolvePreviewPositionPercent21(appObj, st, overrideValue) {
    var raw = null;
    if (overrideValue !== undefined && overrideValue !== null) raw = overrideValue;
    else if (st && st.positionPreviewPercent !== undefined && st.positionPreviewPercent !== null) raw = st.positionPreviewPercent;
    else {
      try { raw = appObj && appObj.config ? appObj.config.POINTER_RESULT_PREVIEW_POSITION_PERCENT : null; } catch (eConfig) { raw = null; }
    }
    var value = Number(raw);
    if (isNaN(value)) value = 5;
    return Math.round(clamp21(value, 0, 100));
  }

  function calculatePreviewTopY21(appObj, st, overrideValue) {
    var frame = screenFrame21(appObj);
    var insets = systemSafeInsets21(st && st.root ? st.root : null);
    var previewHeight = 0;
    try { previewHeight = Math.max(previewHeight, int21(st && st.measuredHeight, 0)); } catch (eMeasured) {}
    try { previewHeight = Math.max(previewHeight, int21(st && st.lp ? st.lp.height : 0, 0)); } catch (eLpHeight) {}
    try { previewHeight = Math.max(previewHeight, int21(st && st.root ? st.root.getHeight() : 0, 0)); } catch (eRootHeight) {}
    previewHeight = Math.max(1, previewHeight);
    var percent = resolvePreviewPositionPercent21(appObj, st, overrideValue);
    var targetCenterY = frame.top + Math.round(frame.height * percent / 100);
    var candidateTopY = Math.round(targetCenterY - previewHeight / 2);
    var minTopY = frame.top + insets.top;
    var maxTopY = frame.bottom - insets.bottom - previewHeight;
    var finalTopY = candidateTopY;
    var degraded = false;
    if (maxTopY >= minTopY) {
      finalTopY = Math.round(clamp21(candidateTopY, minTopY, maxTopY));
    } else {
      degraded = true;
      finalTopY = Math.round(clamp21(candidateTopY, frame.top, Math.max(frame.top, frame.bottom - previewHeight)));
    }
    return {
      percent: percent,
      screenHeight: frame.height,
      screenSource: frame.source,
      previewHeight: previewHeight,
      safeTop: insets.top,
      safeBottom: insets.bottom,
      insetSource: insets.source,
      targetCenterY: targetCenterY,
      candidateTopY: candidateTopY,
      finalTopY: finalTopY,
      clamped: finalTopY !== candidateTopY,
      degraded: degraded
    };
  }

  function applyPreviewPosition21(appObj, st, options) {
    if (!st) return false;
    var opts = options || {};
    if (opts.clearPositionPreview === true) st.positionPreviewPercent = null;
    if (opts.positionPreviewPercent !== undefined && opts.positionPreviewPercent !== null) {
      st.positionPreviewPercent = resolvePreviewPositionPercent21(appObj, st, opts.positionPreviewPercent);
    }
    if (!st.lp) return false;
    var info = calculatePreviewTopY21(appObj, st, null);
    var oldY = int21(st.lp.y, 0);
    st.lp.y = info.finalTopY;
    var changed = oldY !== info.finalTopY;
    if (opts.updateLayout !== false && changed && st.added && st.root && st.wm) {
      try { st.wm.updateViewLayout(st.root, st.lp); } catch (eUpdate) {
        try { safeLog(appObj.L, "w", "result preview position update fail reason=" + String(opts.reason || "") + " err=" + String(eUpdate)); } catch (eLogUpdate) {}
        return false;
      }
    }
    if (changed || info.clamped || info.degraded || opts.forceLog === true) {
      try {
        safeLog(appObj.L, info.degraded ? "w" : "i",
          "result preview position" +
          " reason=" + String(opts.reason || "") +
          " percent=" + String(info.percent) +
          " screenH=" + String(info.screenHeight) +
          " screenSource=" + String(info.screenSource || "") +
          " previewH=" + String(info.previewHeight) +
          " safeTop=" + String(info.safeTop) +
          " safeBottom=" + String(info.safeBottom) +
          " insetSource=" + String(info.insetSource || "") +
          " targetCenterY=" + String(info.targetCenterY) +
          " candidateY=" + String(info.candidateTopY) +
          " finalY=" + String(info.finalTopY) +
          " clamped=" + String(info.clamped === true) +
          " degraded=" + String(info.degraded === true));
      } catch (eLog) {}
    }
    return true;
  }

  function cancelConfigurationPosition21(st) {
    if (!st) return;
    try {
      if (st.handler && st.configurationPositionRunnable) st.handler.removeCallbacks(st.configurationPositionRunnable);
    } catch (eRemove) {}
    st.configurationPositionRunnable = null;
  }

  function scheduleStablePosition21(appObj, st, reason) {
    if (!st || !st.handler || !st.root || !st.added) return false;
    cancelConfigurationPosition21(st);
    var rootRef = st.root;
    var rootToken = Number(st.rootToken || 0);
    st.configurationPositionRunnable = new java.lang.Runnable({ run: function() {
      try {
        st.configurationPositionRunnable = null;
        if (!appObj.state || appObj.state.closing === true || appObj.state.closed === true) return;
        if (st.root !== rootRef || Number(st.rootToken || 0) !== rootToken || st.added !== true) return;
        applyPreviewPosition21(appObj, st, { reason: String(reason || "configuration") + ":stable" });
      } catch (eStable) {
        try { safeLog(appObj.L, "w", "result preview stable position fail: " + String(eStable)); } catch (eLog) {}
      }
    }});
    try { return st.handler.postDelayed(st.configurationPositionRunnable, 280) === true; } catch (ePost) {}
    st.configurationPositionRunnable = null;
    return false;
  }

  function screenWidth21(appObj) {'''
          sub_once(
              p,
              r"  function topInset21\(root\) \{.*?\n  function screenWidth21\(appObj\) \{",
              position_helpers,
              flags=re.S
          )

          replace_once(p, "    lp.y = topInset21(st.root) + dp21(appObj, 8);", "    lp.y = 0;")
          replace_once(p, "    try { rootRef.setTranslationY(-dp21(appObj, 6)); } catch (eTranslation) {}", "    try { rootRef.setTranslationY(0); } catch (eTranslation) {}")
          replace_once(
              p,
              '''    st.lp.width = st.measuredWidth;
    st.lp.height = st.measuredHeight;
    st.lp.y = topInset21(st.root) + dp21(appObj, 8);''',
              '''    st.lp.width = st.measuredWidth;
    st.lp.height = st.measuredHeight;
    applyPreviewPosition21(appObj, st, { reason: "attach", updateLayout: false });'''
          )
          replace_once(p, "    st.root.setTranslationY(-dp21(appObj, 6));", "    st.root.setTranslationY(0);")
          replace_once(
              p,
              '''    st.lp.width = st.measuredWidth;
    st.lp.height = st.measuredHeight;
    st.lp.y = topInset21(rootRef) + dp21(appObj, 8);''',
              '''    st.lp.width = st.measuredWidth;
    st.lp.height = st.measuredHeight;
    applyPreviewPosition21(appObj, st, { reason: "content_update", updateLayout: false });'''
          )
          replace_once(
              p,
              '''      rootRef.invalidate();
      if (rootRef.postInvalidateOnAnimation) rootRef.postInvalidateOnAnimation();
      rootRef.setScaleX(0.985);''',
              '''      rootRef.invalidate();
      if (rootRef.postInvalidateOnAnimation) rootRef.postInvalidateOnAnimation();
      rootRef.setAlpha(0.82);
      rootRef.setScaleX(0.985);'''
          )
          replace_once(
              p,
              '''      rootRef.animate()
        .scaleX(1)
        .scaleY(1)
        .setDuration(120)''',
              '''      rootRef.animate()
        .alpha(1)
        .scaleX(1)
        .scaleY(1)
        .setDuration(120)'''
          )
          replace_once(p, "    cancelCopyFeedback21(st);\n    var rootRef = st.root;", "    cancelCopyFeedback21(st);\n    cancelConfigurationPosition21(st);\n    var rootRef = st.root;")

          enter_animation = r'''  function startEnterAnimation21(appObj, st, rootRef, render) {
    if (!isCurrentRoot21(st, rootRef, render)) return false;
    if (render.enterStarted === true) return true;
    render.enterStarted = true;
    try {
      rootRef.animate().cancel();
      rootRef.setVisibility(android.view.View.VISIBLE);
      rootRef.setAlpha(0.76);
      rootRef.setScaleX(0.985);
      rootRef.setScaleY(0.985);
      rootRef.setTranslationY(0);
      rootRef.animate()
        .alpha(1)
        .scaleX(1)
        .scaleY(1)
        .setDuration(140)
        .setInterpolator(new android.view.animation.DecelerateInterpolator())
        .start();
      return true;
    } catch (eEnter) {
      try {
        rootRef.setVisibility(android.view.View.VISIBLE);
        rootRef.setAlpha(1);
        rootRef.setScaleX(1);
        rootRef.setScaleY(1);
        rootRef.setTranslationY(0);
      } catch (eFallback) {}
    }
    return false;
  }

  function markFirstDraw21(appObj, st, rootRef, render) {'''
          sub_once(
              p,
              r"  function startEnterAnimation21\(appObj, st, rootRef, render\) \{.*?\n  function markFirstDraw21\(appObj, st, rootRef, render\) \{",
              enter_animation,
              flags=re.S
          )
          replace_once(p, "                .translationY(-dp21(self, 6))", "                .alpha(0)")

          config_method = r'''      proto.onResultPreviewConfigurationChanged = function(options) {
        var opts = options || {};
        var self = this;
        var st = ensureState21(this);
        if (!st) return false;
        if (opts.clearPositionPreview === true) st.positionPreviewPercent = null;
        if (opts.positionPreviewPercent !== undefined && opts.positionPreviewPercent !== null) {
          st.positionPreviewPercent = resolvePreviewPositionPercent21(this, st, opts.positionPreviewPercent);
        }
        if (opts.positionOnly === true) {
          runOnMain21(function() {
            try {
              var currentPosition = ensureState21(self);
              if (!currentPosition) return;
              applyPreviewPosition21(self, currentPosition, {
                reason: String(opts.reason || "position_only"),
                clearPositionPreview: opts.clearPositionPreview === true,
                positionPreviewPercent: opts.positionPreviewPercent
              });
            } catch (ePositionOnly) {
              try { safeLog(self.L, "w", "result preview position-only update fail: " + String(ePositionOnly)); } catch (eLogPositionOnly) {}
            }
          });
          return true;
        }
        if (!st.payload) return false;
        cancelCopyFeedback21(st);
        runOnMain21(function() {
          try {
            var current = ensureState21(self);
            if (!current || !current.payload || !current.root || !current.added || !current.rootRender) return;
            current.generation = Number(current.generation || 0) + 1;
            prepareLines21(self, current);
            syncRender21(current.rootRender, current);
            current.rootRender.rootToken = Number(current.rootToken || 0);
            current.rootRender.enterStarted = true;
            current.lp.width = current.measuredWidth;
            current.lp.height = current.measuredHeight;
            applyPreviewPosition21(self, current, {
              reason: String(opts.reason || "configuration_changed"),
              updateLayout: false
            });
            current.wm.updateViewLayout(current.root, current.lp);
            current.root.requestLayout();
            current.root.invalidate();
            if (current.root.postInvalidateOnAnimation) current.root.postInvalidateOnAnimation();
            scheduleVisibilityFallback21(self, current, current.root, current.rootRender, 1);
            scheduleStablePosition21(self, current, String(opts.reason || "configuration_changed"));
          } catch (eReflow) {
            try { safeLog(self.L, 'w', "result preview reflow fail: " + String(eReflow)); } catch (eLog) {}
          }
        });
        return true;
      };'''
          sub_once(
              p,
              r"      proto\.onResultPreviewConfigurationChanged = function\(\) \{.*?\n      \};\n\n      proto\.__toolHubResultPreviewInstalled",
              config_method + "\n\n      proto.__toolHubResultPreviewInstalled",
              flags=re.S
          )

          # Verification contract now checks percentage positioning, navigation-bar safety and fade/scale-only motion.
          p = "scripts/verify_result_preview.py"
          replace_once(
              p,
              '''    require(
        "preview / status bar and cutout inset",
        "WindowInsets.Type.statusBars()" in preview
        and "WindowInsets.Type.displayCutout()" in preview
        and 'getIdentifier("status_bar_height"' in preview,
        "top position must respect status bar and cutout safe insets",
        failures,
    )''',
              '''    require(
        "preview / full-screen percentage and system-bar safety",
        "POINTER_RESULT_PREVIEW_POSITION_PERCENT" in base
        and "function screenFrame21(appObj)" in preview
        and "function calculatePreviewTopY21(appObj, st, overrideValue)" in preview
        and "frame.height * percent / 100" in preview
        and "WindowInsets.Type.statusBars()" in preview
        and "WindowInsets.Type.navigationBars()" in preview
        and "WindowInsets.Type.displayCutout()" in preview
        and "getInsetsIgnoringVisibility" in preview
        and 'getIdentifier("status_bar_height"' in preview
        and 'getIdentifier("navigation_bar_height"' in preview,
        "preview position must use complete screen height, then clamp against top and bottom system insets",
        failures,
    )'''
          )
          replace_once(
              p,
              '''    require(
        "preview / opaque motion animation",
        "rootRef.setAlpha(1)" in preview
        and ".alpha(0)" not in preview
        and ".scaleX(0.97).scaleY(0.97)" in preview
        and ".translationY(-dp21(self, 6))" in preview
        and ".setDuration(120)" in preview
        and "st.clickLocked" in preview,
        "preview entry, primary touch and exit feedback must not animate whole-window alpha",
        failures,
    )''',
              '''    require(
        "preview / fade and scale only motion",
        ".alpha(1)" in preview
        and ".alpha(0)" in preview
        and ".scaleX(0.97).scaleY(0.97)" in preview
        and "translationY(-dp21" not in preview
        and "setTranslationY(0)" in preview
        and ".setDuration(120)" in preview
        and "st.clickLocked" in preview,
        "preview motion must use alpha and scale without vertical translation",
        failures,
    )'''
          )

          # Sanity checks for the new contract.
          required = {
              "code/th_01_base.js": [
                  "POINTER_RESULT_PREVIEW_POSITION_PERCENT",
                  "预览垂直位置(%)",
              ],
              "code/th_13_panel_ui.js": ["settings_seek_stop"],
              "code/th_14_panels.js": ["POINTER_RESULT_PREVIEW_POSITION_PERCENT"],
              "code/th_21_result_preview.js": [
                  "calculatePreviewTopY21",
                  "navigationBars()",
                  "getInsetsIgnoringVisibility",
                  "positionOnly",
                  "configurationPositionRunnable",
              ],
          }
          for path, needles in required.items():
              text = load(path)
              for needle in needles:
                  if needle not in text:
                      raise SystemExit(f"{path}: missing {needle}")
          PY

      - name: Validate JavaScript and contracts
        shell: bash
        run: |
          node --check code/th_01_base.js
          node --check code/th_05_persistence.js
          node --check code/th_09_animation.js
          node --check code/th_13_panel_ui.js
          node --check code/th_14_panels.js
          node --check code/th_16_entry.js
          node --check code/th_21_result_preview.js
          python3 scripts/verify_result_preview.py
          python3 scripts/verify_schema_validator.py

      - name: Commit patched files and remove one-shot workflow
        shell: bash
        run: |
          rm -f .github/workflows/apply-result-preview-position-percent.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add code/th_01_base.js code/th_05_persistence.js code/th_09_animation.js code/th_13_panel_ui.js code/th_14_panels.js code/th_16_entry.js code/th_21_result_preview.js scripts/verify_result_preview.py .github/workflows/apply-result-preview-position-percent.yml
          git commit -m "新增结果预览垂直位置百分比"
          git push origin HEAD:agent/result-preview-position-percent
