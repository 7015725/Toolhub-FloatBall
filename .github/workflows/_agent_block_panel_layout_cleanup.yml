name: panel-layout-cleanup-pending

on:
  pull_request:
    branches: [main]

jobs:
  pending:
    if: github.event.pull_request.head.ref == 'agent/panel-layout-position-settings-20260714'
    runs-on: ubuntu-latest
    steps:
      - name: Wait for formal signed patch
        run: |
          echo "Formal signed patch has not been committed yet"
          exit 1
