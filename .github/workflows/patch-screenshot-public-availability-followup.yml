name: patch-screenshot-public-availability-followup

on:
  push:
    branches: [agent/fix-screenshot-public-availability]

permissions:
  contents: write

concurrency:
  group: patch-screenshot-public-availability-followup
  cancel-in-progress: true

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout fix branch
        uses: actions/checkout@v4
        with:
          ref: agent/fix-screenshot-public-availability
          fetch-depth: 0
          token: ${{ secrets.TOOLHUB_AUTOMATION_TOKEN || github.token }}
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Apply follow-up patch
        run: python3 .github/scripts/patch_screenshot_public_availability_followup.py

      - name: Fetch main baseline
        run: git fetch --no-tags origin main

      - name: Verify changed module versions
        run: python3 scripts/verify_changed_module_versions.py --base-ref origin/main

      - name: Verify pending update record
        run: python3 scripts/verify_update_history.py --require-one-pending

      - name: Verify ES5 scan
        run: python3 .github/scripts/es5_scan.py

      - name: Verify JavaScript syntax
        run: python3 scripts/verify_js_syntax.py

      - name: Verify image viewer
        run: python3 scripts/verify_pickword_image_viewer.py

      - name: Verify screenshot manager
        run: python3 scripts/verify_screenshot_manager.py

      - name: Verify module boundaries
        run: python3 scripts/verify_module_boundaries.py

      - name: Verify whitespace
        run: git diff --check

      - name: Remove one-shot follow-up files
        run: |
          rm -f .github/scripts/patch_screenshot_public_availability_followup.py
          rm -f .github/workflows/patch-screenshot-public-availability-followup.yml

      - name: Commit follow-up fix
        env:
          TOOLHUB_AUTOMATION_TOKEN: ${{ secrets.TOOLHUB_AUTOMATION_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "收口截图公共副本访问视图边界"
          if [ -n "$TOOLHUB_AUTOMATION_TOKEN" ]; then
            git remote set-url origin "https://x-access-token:${TOOLHUB_AUTOMATION_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          fi
          git push origin HEAD:agent/fix-screenshot-public-availability
