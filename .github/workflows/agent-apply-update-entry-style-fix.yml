name: agent-apply-update-entry-style-fix

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'fix/update-entry-style-consistency'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/update-entry-style-consistency
          fetch-depth: 0

      - name: Apply fix
        run: python3 .agent/apply_update_entry_style_fix.py

      - name: Remove temporary patch script
        run: rm -f .agent/apply_update_entry_style_fix.py && rmdir .agent 2>/dev/null || true

      - name: Verify JavaScript and module version
        run: |
          python3 .github/scripts/es5_scan.py
          python3 scripts/verify_js_syntax.py
          python3 scripts/verify_changed_module_versions.py --base-ref origin/main

      - name: Verify settings entry style
        run: |
          python3 scripts/verify_update_version_page.py
          python3 scripts/verify_settings_color_scheme.py
          python3 scripts/verify_settings_color_roles.py
          python3 scripts/verify_module_boundaries.py
          python3 scripts/report_dead_module_symbols.py --check DEAD_CODE_AUDIT.md

      - name: Verify diff
        run: git diff --check

      - name: Commit fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes"
            exit 1
          fi
          git commit -m "统一更新与版本入口列表样式"
          git push origin HEAD:fix/update-entry-style-consistency
